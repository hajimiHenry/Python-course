"""
*args 和 **kwargs 互动体验
运行这个文件，跟着提示一步步操作，亲手感受它们的用法！
"""

# ============================================================
# 第一部分：*args —— 把一堆值打包成元组
# ============================================================


def add(*args):
    """把传进来的所有数字加起来"""
    print(f"  → args 收到的内容: {args}")
    print(f"  → args 的类型: {type(args).__name__}")
    total = 0
    for val in args:
        total += val
    return total


print("=" * 50)
print("🎯 第一部分：体验 *args")
print("=" * 50)

print("\n--- 传2个数: add(1, 2) ---")
result = add(1, 2)
print(f"  → 结果: {result}")

print("\n--- 传5个数: add(10, 20, 30, 40, 50) ---")
result = add(10, 20, 30, 40, 50)
print(f"  → 结果: {result}")

print("\n--- 什么都不传: add() ---")
result = add()
print(f"  → 结果: {result}")

input("\n按回车继续...\n")


# ============================================================
# 第二部分：**kwargs —— 把 名字=值 打包成字典
# ============================================================


def introduce(**kwargs):
    """把传进来的所有信息打印出来"""
    print(f"  → kwargs 收到的内容: {kwargs}")
    print(f"  → kwargs 的类型: {type(kwargs).__name__}")
    print("  → 逐个拆开看:")
    for key in kwargs:
        print(f"     {key} = {kwargs[key]}")


print("=" * 50)
print("🎯 第二部分：体验 **kwargs")
print("=" * 50)

print("\n--- 传3个信息: introduce(name='小明', age=18, hobby='打游戏') ---")
introduce(name="小明", age=18, hobby="打游戏")

print("\n--- 只传1个: introduce(name='小红') ---")
introduce(name="小红")

input("\n按回车继续...\n")


# ============================================================
# 第三部分：*args 和 **kwargs 一起用
# ============================================================


def show_all(*args, **kwargs):
    """同时接收位置参数和关键字参数"""
    print(f"  → 没名字的(args):   {args}")
    print(f"  → 有名字的(kwargs): {kwargs}")


print("=" * 50)
print("🎯 第三部分：*args + **kwargs 一起用")
print("=" * 50)

print("\n--- show_all(1, 2, 3, name='小明', age=18) ---")
show_all(1, 2, 3, name="小明", age=18)

print("\n--- show_all(True, 3.14, city='北京', food='火锅') ---")
show_all(True, 3.14, city="北京", food="火锅")

input("\n按回车继续...\n")


# ============================================================
# 第四部分：自己动手试试！
# ============================================================

print("=" * 50)
print("🎮 第四部分：轮到你了！")
print("=" * 50)
print()
print("现在你可以在下方输入数字，用 add() 函数来求和")
print("输入格式：用空格隔开，例如: 1 2 3 4 5")
print("输入 q 退出\n")

while True:
    user_input = input("请输入一组数字（空格隔开）: ")
    if user_input.strip().lower() == "q":
        print("再见！👋")
        break
    try:
        numbers = [float(x) for x in user_input.split()]
        # 用 * 拆开列表，传给 add 函数
        result = add(*numbers)
        print(f"  → 总和 = {result}\n")
    except ValueError:
        print("  ⚠️ 请输入数字哦，用空格隔开！\n")
